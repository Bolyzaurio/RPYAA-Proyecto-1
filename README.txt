
compilado con
gcc nasa_shuttle.c -lgsl -lm -Wall -pedantic

Si no esta instalada la biblioteca de GSL instalar.
(GNU Scientific Library)

Usar Make File

make

Existe un bug en el programa donde no finalizo un puntero y crea un error pero no es importante ya que,
finaliza las rutinas de Naive Bayes

Existe un error en el cual GSL no puede obtener suficiente espacio en memoria por lo que falla, puede hacer falta
correr el programa varias veces.

